export function createMaritimeTranscriptAnalysisModel({
  strategyName,
  model,
  hasMaritimeCommandInformation,
  classification,
  recommendedHumanReviewLabel = "",
  vesselInformation,
  commandInformation,
  spokenLanguageProportions = null,
  confidence,
  analysisDescription = "",
  analysisRemarks = {},
  transcriptionTiming = null,
  analysisTiming = null,
  debugContext,
  llmResponseText = "",
  rawResponse,
}) {
  return Object.freeze({
    strategyName,
    model,
    hasMaritimeCommandInformation,
    classification,
    recommendedHumanReviewLabel,
    vesselInformation,
    commandInformation,
    spokenLanguageProportions,
    confidence,
    analysisDescription,
    analysisRemarks,
    transcriptionTiming,
    analysisTiming,
    debugContext,
    llmResponseText,
    rawResponse,
  });
}
